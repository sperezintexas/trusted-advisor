# Copy to .env at repo root and set values. Do not commit .env.
# Both backend (Spring Boot) and frontend (Next.js) read this file.
# MongoDB: set MONGODB_URI or MONGODB_URI_B64 (base64-encoded URI, useful when URI has special chars or for secrets).
# For Atlas: mongodb+srv://user:pass@cluster.mongodb.net/trusted-advisor (or set MONGODB_DATABASE if URI has no path).
# Encode URI: echo -n "mongodb+srv://..." | base64
MONGODB_URI=mongodb://localhost:27017/trusted-advisor
# MONGODB_URI_B64=<base64-encoded-uri>
MONGODB_DATABASE=trusted-advisor
XAI_API_KEY=
